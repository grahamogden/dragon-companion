import type { ThemeDefinition } from 'vuetify'
import { timberwolf50, woodsmoke950 } from './variables'

export const customDark: ThemeDefinition = {
    dark: true,
    colors: {
        'text-default': timberwolf50,
        'bg-input-text-field': woodsmoke950,
    },
}
