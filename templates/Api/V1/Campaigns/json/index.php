<?php

declare(strict_types=1);

// use App\Model\Entity\Campaign;

// /** @var Campaign $campaign */
// foreach ($campaigns as $campaign) {
//     unset($campaign->_matchingData);
// }
// dd($campaigns);

echo json_encode($campaigns);//compact('campaigns')['campaigns']);
